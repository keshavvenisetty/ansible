- name: play level variables
  hosts: local
  connection: local
  vars: 
   COURSE: DevOps with AWS   # in shell we refer variables $VAR or ${VAR}
   DURATION: 10hrs
   TRAINER: Shivakumar
  tasks:
  - name: print course information
    vars:
      COURSE: Ansible
    ansible.builtin.debug:
    #vars 
        msg: "Hello {{ COURSE }}, Course Duration: {{ DURATION }}, Trainer is {{ TRAINER }}"

  - name: print course information
    ansible.builtin.debug:
    #vars 
        msg: "Hello {{ COURSE }}, Course Duration: {{ DURATION }}, Trainer is {{ TRAINER }}"
